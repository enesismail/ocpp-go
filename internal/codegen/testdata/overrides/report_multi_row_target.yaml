version: v201
tagOverrides:
  - definition: BootNotificationResponse
    property: interval
    tag: gte=0
    rule: add
    rationale: The schema declares interval as an integer with no minimum, so a negative value validates; a negative heartbeat interval cannot be scheduled from, and this boundary rejects it even though the published schema accepts it.
    source: "OCPP 2.0.1 Part 2, BootNotification response — interval is the number of seconds between heartbeats"
    author: "Generator maintainer"
    date: 2026-08-08
  - definition: BootNotificationResponse
    property: interval
    tag: lte=86400
    rule: add
    rationale: The schema declares no maximum either, so an interval longer than a day validates; this boundary keeps the heartbeat within the operational range even though the published schema accepts a longer one.
    source: "OCPP 2.0.1 Part 2, BootNotification response — interval is the number of seconds between heartbeats"
    author: "Generator maintainer"
    date: 2026-08-08
dedupAllowlist: []
